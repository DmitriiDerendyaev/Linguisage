# Используйте официальный образ OpenJDK для Java 17
FROM adoptopenjdk:17-jre-openj9

# Создайте рабочий каталог внутри контейнера
WORKDIR /app

# Скопируйте JAR-файл вашего приложения в рабочий каталог
COPY target/Imagenius-0.0.1-SNAPSHOT.jar app.jar

# Задайте переменную среды для порта вашего приложения (измените на необходимый порт)
ENV SERVER_PORT=8080

# Запустите приложение при запуске контейнера
CMD ["java", "-jar", "app.jar"]
